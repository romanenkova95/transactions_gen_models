#! /bin/bash
source credentials

docker run \
 -d \
 -p ${CONTAINER_PORT}:8890 \
 --shm-size=16g \
 --memory=128g \
 --cpus=2 \
 --user root:root \
 --name ${CONTAINER_NAME} \
 --rm -it --init \
 -v /home/${USER}/${SRC}:/app \
 --gpus '"device=3"' \
 ${DOCKER_NAME} bash
